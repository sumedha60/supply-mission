class package{
    constructor (){
    Matter .body.setStatic(packageBody,isStatic)
      }

      display(){
        push ()
        rectMode(CENTER)
        translate (this.body.position.x,this.body.position.y)
        rotate (packageBody.body.angle)
        rect(0,0,this.width,this.height)
      
        pop ()
      }

}